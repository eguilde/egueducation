import { useCallback, useEffect, useMemo, useState, type ChangeEvent } from "react";
import { Button } from "@primereact/ui/button";
import { Card } from "@primereact/ui/card";
import { DataTable } from "@primereact/ui/datatable";
import { Dialog } from "@primereact/ui/dialog";
import { InputText } from "@primereact/ui/inputtext";
import { Message } from "@primereact/ui/message";
import { ProgressSpinner } from "@primereact/ui/progressspinner";
import { Select, type SelectValueChangeEvent } from "@primereact/ui/select";
import { Tag } from "@primereact/ui/tag";
import type { ContractClient } from "../../api/client";
import type { components } from "../../api/generated";

type Package = components["schemas"]["PortfolioValorificationPackage"];
type PackagePage = components["schemas"]["EducationPageOfPortfolioValorificationPackage"];
type Scope = components["schemas"]["CreatePortfolioValorificationPackageRequest"]["scope"];
type Source = components["schemas"]["PortfolioValorificationEligibleSource"];
type ArchiveVersion = components["schemas"]["PortfolioValorificationEligibleArchiveVersion"];
type Evidence = components["schemas"]["PortfolioValorificationPackageDocument"];
export type ValorificationCapabilities = { read: boolean; manage: boolean };
export type ValorificationQuery = { page: number; pageSize: number; sort?: string; direction?: "asc" | "desc"; filters?: { scope?: string; status?: string } };
export type PortfolioValorificationPackageApi = {
  list(portfolioID: string, query: ValorificationQuery): Promise<PackagePage>;
  sources(portfolioID: string, scope: Scope): Promise<Source[]>;
  archiveVersions(portfolioID: string): Promise<ArchiveVersion[]>;
  create(portfolioID: string, scope: Scope, sourceID: string): Promise<Package>;
  attach(portfolioID: string, packageID: string, version: ArchiveVersion): Promise<void>;
  advance(portfolioID: string, packageID: string, action: "submit" | "validate" | "complete"): Promise<Package>;
  evidence(portfolioID: string, packageID: string): Promise<Evidence[]>;
};

const requireData = async <T,>(result: { data?: T; error?: unknown; response: Response }): Promise<T> => { if (result.data !== undefined) return result.data; throw new Error(`valorification_${result.response.status}`); };
const queryParams = (query: ValorificationQuery) => ({ page: query.page, pageSize: query.pageSize, sort: query.sort, direction: query.direction, "filter.scope": query.filters?.scope || undefined, "filter.status": query.filters?.status || undefined });
/** OpenAPI-generated client adapter; package and evidence IDs are always server-issued. */
export function createPortfolioValorificationPackageApi(client: ContractClient): PortfolioValorificationPackageApi { return {
  list: async (portfolioID, query) => requireData(await client.GET("/api/education/portfolios/records/{recordID}/valorification-packages", { params: { path: { recordID: portfolioID }, query: queryParams(query) } })),
  sources: async (portfolioID, scope) => requireData(await client.GET("/api/education/portfolios/records/{recordID}/valorification-packages/eligible-sources", { params: { path: { recordID: portfolioID }, query: { scope } } })),
  archiveVersions: async (portfolioID) => requireData(await client.GET("/api/education/portfolios/records/{recordID}/valorification-packages/eligible-archive-versions", { params: { path: { recordID: portfolioID } } })),
  create: async (portfolioID, scope, sourceID) => requireData(await client.POST("/api/education/portfolios/records/{recordID}/valorification-packages", { params: { path: { recordID: portfolioID } }, body: scope === "evaluare_profesionala" ? { scope, source_evaluation_id: sourceID } : scope === "mobilitate" ? { scope, source_mobility_case_id: sourceID } : { scope, source_merit_grant_id: sourceID } })),
  attach: async (portfolioID, packageID, version) => { await requireData(await client.POST("/api/education/portfolios/records/{recordID}/valorification-packages/{itemID}/documents", { params: { path: { recordID: portfolioID, itemID: packageID } }, body: { archive_document_id: version.archive_document_id, archive_version_id: version.archive_version_id } })); },
  advance: async (portfolioID, packageID, action) => requireData(await client.POST("/api/education/portfolios/records/{recordID}/valorification-packages/{itemID}/advance", { params: { path: { recordID: portfolioID, itemID: packageID } }, body: { action } })),
  evidence: async (portfolioID, packageID) => requireData(await client.GET("/api/education/portfolios/records/{recordID}/valorification-packages/{itemID}/documents", { params: { path: { recordID: portfolioID, itemID: packageID } } })),
}; }

const scopes: Array<{ value: Scope; label: string }> = [{ value: "evaluare_profesionala", label: "Evaluare profesională" }, { value: "mobilitate", label: "Mobilitate" }, { value: "gradatie_merit", label: "Gradație de merit" }];
const statusSeverity = (status: Package["status"]): "secondary" | "warn" | "success" => status === "completed" ? "success" : status === "draft" ? "secondary" : "warn";
const statusLabel = (status: string) => ({ draft: "Ciornă", submitted: "Depus", validated: "Validat", completed: "Finalizat" }[status] ?? status);
const queryInitial = (): ValorificationQuery => ({ page: 1, pageSize: 20, filters: {} });

function CreatePackageWizard({ portfolioID, api, onClose, onSaved }: { portfolioID: string; api: PortfolioValorificationPackageApi; onClose: () => void; onSaved: () => Promise<void> }) {
  const [scope, setScope] = useState<Scope>("evaluare_profesionala"); const [sources, setSources] = useState<Source[]>([]); const [sourceID, setSourceID] = useState(""); const [created, setCreated] = useState<Package>(); const [versions, setVersions] = useState<ArchiveVersion[]>([]); const [versionID, setVersionID] = useState(""); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState<string>();
  useEffect(() => { let active = true; setLoading(true); setSourceID(""); setError(undefined); void api.sources(portfolioID, scope).then((items) => { if (active) setSources(items); }).catch(() => { if (active) setError("Sursele eligibile nu au putut fi încărcate."); }).finally(() => { if (active) setLoading(false); }); return () => { active = false; }; }, [api, portfolioID, scope]);
  const create = async () => { if (!sourceID) return; setSaving(true); setError(undefined); try { const item = await api.create(portfolioID, scope, sourceID); setCreated(item); setLoading(true); setVersions(await api.archiveVersions(portfolioID)); } catch { setError("Pachetul nu a putut fi creat."); } finally { setSaving(false); setLoading(false); } };
  const attach = async () => { const version = versions.find((item) => item.archive_version_id === versionID); if (!created || !version) return; setSaving(true); setError(undefined); try { await api.attach(portfolioID, created.id, version); await onSaved(); onClose(); } catch { setError("Versiunea arhivată nu a putut fi atașată."); } finally { setSaving(false); } };
  return <Dialog.Root open onOpenChange={(event: { value?: boolean }) => !event.value && !saving && onClose()}><Dialog.Portal><Dialog.Backdrop /><Dialog.Positioner><Dialog.Popup className="w-[min(96vw,42rem)]"><Dialog.Header><Dialog.Title>{created ? "Atașează versiune eArhivă" : "Creează pachet de valorificare"}</Dialog.Title><Dialog.Close aria-label="Închide asistentul" /></Dialog.Header><Dialog.Content><div className="flex flex-col gap-3">{error && <Message.Root severity="error"><Message.Content><Message.Text>{error}</Message.Text></Message.Content></Message.Root>}<Message.Root severity="info"><Message.Content><Message.Text>{created ? "Alegeți o versiune eligibilă. Identificatorul de versiune și amprenta probatorie sunt păstrate de server." : "Selectați domeniul și sursa eligibilă furnizată de server."}</Message.Text></Message.Content></Message.Root>{loading ? <div className="flex justify-center p-4"><ProgressSpinner.Root><ProgressSpinner.Range><ProgressSpinner.Track /><ProgressSpinner.Value /></ProgressSpinner.Range></ProgressSpinner.Root></div> : created ? <label className="flex flex-col gap-1"><span>Versiune eArhivă *</span><Select.Root value={versionID || null} options={versions.map((item) => ({ label: `${item.title} · versiunea ${item.version_no}`, value: item.archive_version_id }))} optionLabel="label" optionValue="value" onValueChange={(event: SelectValueChangeEvent) => setVersionID(String(event.value ?? ""))}><Select.Trigger aria-label="Versiune eArhivă"><Select.Value placeholder="Alegeți o versiune eligibilă" /><Select.Indicator /></Select.Trigger><Select.Portal><Select.Positioner><Select.Popup><Select.List /></Select.Popup></Select.Positioner></Select.Portal></Select.Root></label> : <><label className="flex flex-col gap-1"><span>Domeniu *</span><Select.Root value={scope} options={scopes} optionLabel="label" optionValue="value" onValueChange={(event: SelectValueChangeEvent) => setScope(event.value as Scope)}><Select.Trigger aria-label="Domeniu valorificare"><Select.Value /><Select.Indicator /></Select.Trigger><Select.Portal><Select.Positioner><Select.Popup><Select.List /></Select.Popup></Select.Positioner></Select.Portal></Select.Root></label><label className="flex flex-col gap-1"><span>Sursă eligibilă *</span><Select.Root value={sourceID || null} options={sources.map((item) => ({ label: item.label, value: item.id }))} optionLabel="label" optionValue="value" onValueChange={(event: SelectValueChangeEvent) => setSourceID(String(event.value ?? ""))}><Select.Trigger aria-label="Sursă eligibilă"><Select.Value placeholder={sources.length ? "Alegeți sursa" : "Nu există surse eligibile"} /><Select.Indicator /></Select.Trigger><Select.Portal><Select.Positioner><Select.Popup><Select.List /></Select.Popup></Select.Positioner></Select.Portal></Select.Root></label></>}</div></Dialog.Content><Dialog.Footer><div className="flex flex-wrap justify-end gap-2"><Button severity="secondary" variant="outlined" disabled={saving} onClick={onClose}>Renunță</Button>{created ? <Button disabled={saving || !versionID} onClick={() => void attach()}>{saving ? "Se atașează…" : "Atașează versiunea"}</Button> : <Button disabled={saving || loading || !sourceID} onClick={() => void create()}>{saving ? "Se creează…" : "Continuă"}</Button>}</div></Dialog.Footer></Dialog.Popup></Dialog.Positioner></Dialog.Portal></Dialog.Root>;
}

function EvidenceDialog({ portfolioID, packageID, api, onClose }: { portfolioID: string; packageID: string; api: PortfolioValorificationPackageApi; onClose: () => void }) { const [items, setItems] = useState<Evidence[]>([]); const [error, setError] = useState<string>(); useEffect(() => { void api.evidence(portfolioID, packageID).then(setItems).catch(() => setError("Dovezile nu au putut fi încărcate.")); }, [api, packageID, portfolioID]); return <Dialog.Root open onOpenChange={(event: { value?: boolean }) => !event.value && onClose()}><Dialog.Portal><Dialog.Backdrop /><Dialog.Positioner><Dialog.Popup className="w-[min(96vw,42rem)]"><Dialog.Header><Dialog.Title>Dovezi arhivate</Dialog.Title><Dialog.Close aria-label="Închide dovezile" /></Dialog.Header><Dialog.Content>{error ? <Message.Root severity="error"><Message.Content><Message.Text>{error}</Message.Text></Message.Content></Message.Root> : <div className="flex flex-col gap-2">{items.length ? items.map((item) => <Card.Root key={item.id}><Card.Body><Card.Content><div className="flex flex-col gap-1"><span>Versiunea {item.archive_version_no}</span><span className="break-all">SHA-256: {item.archive_sha256}</span><span className="break-all">{item.archive_source_bucket}/{item.archive_source_object_key}</span></div></Card.Content></Card.Body></Card.Root>) : <Message.Root severity="info"><Message.Content><Message.Text>Nu există încă dovezi atașate.</Message.Text></Message.Content></Message.Root>}</div>}</Dialog.Content></Dialog.Popup></Dialog.Positioner></Dialog.Portal></Dialog.Root>; }

export function PortfolioValorificationPackageManager({ portfolioID, client, capabilities }: { portfolioID: string; client: ContractClient; capabilities: ValorificationCapabilities }) {
  const api = useMemo(() => createPortfolioValorificationPackageApi(client), [client]); const [query, setQuery] = useState(queryInitial); const [page, setPage] = useState<PackagePage>({ items: [], total: 0, page: 1, pageSize: 20 }); const [loading, setLoading] = useState(false); const [error, setError] = useState<string>(); const [creating, setCreating] = useState(false); const [evidenceID, setEvidenceID] = useState<string>();
  const load = useCallback(async () => { if (!capabilities.read) return; setLoading(true); setError(undefined); try { setPage(await api.list(portfolioID, query)); } catch { setError("Pachetele nu au putut fi încărcate."); } finally { setLoading(false); } }, [api, capabilities.read, portfolioID, query]); useEffect(() => { void load(); }, [load]);
  const advance = async (item: Package, action: "submit" | "validate" | "complete") => { setLoading(true); setError(undefined); try { await api.advance(portfolioID, item.id, action); await load(); } catch { setError("Tranziția nu a putut fi aplicată. Verificați starea pachetului și drepturile."); } finally { setLoading(false); } };
  const lastPage = Math.max(1, Math.ceil(page.total / page.pageSize)); const filters = query.filters ?? {};
  return <Card.Root><Card.Body><Card.Title>Pachete de valorificare</Card.Title><Card.Content><div className="flex min-h-0 flex-col gap-3">{!capabilities.read ? <Message.Root severity="info"><Message.Content><Message.Text>Nu aveți capabilitatea de consultare a pachetelor de valorificare.</Message.Text></Message.Content></Message.Root> : <>{error && <Message.Root severity="error"><Message.Content><Message.Text>{error}</Message.Text></Message.Content></Message.Root>}<div className="min-h-64 overflow-hidden rounded-border border border-surface">{loading && !page.items.length ? <div className="flex min-h-64 items-center justify-center"><ProgressSpinner.Root><ProgressSpinner.Range><ProgressSpinner.Track /><ProgressSpinner.Value /></ProgressSpinner.Range></ProgressSpinner.Root></div> : <DataTable.Root data={page.items as unknown as Record<string, unknown>[]} dataKey="id" scrollable className="max-h-[min(55dvh,36rem)] min-h-56 overflow-auto"><DataTable.Table><DataTable.THead className="sticky top-0 z-10"><DataTable.THeadRow><DataTable.THeadCell><div className="flex flex-col gap-1"><Button size="small" variant="text" severity="secondary" onClick={() => setQuery((current) => ({ ...current, page: 1, sort: "scope", direction: current.sort === "scope" && current.direction === "asc" ? "desc" : "asc" }))}>Domeniu</Button><InputText aria-label="Filtru domeniu" value={filters.scope ?? ""} onChange={(event: ChangeEvent<HTMLInputElement>) => setQuery((current) => ({ ...current, page: 1, filters: { ...current.filters, scope: event.target.value } }))} placeholder="Filtru" /></div></DataTable.THeadCell><DataTable.THeadCell><div className="flex flex-col gap-1"><Button size="small" variant="text" severity="secondary" onClick={() => setQuery((current) => ({ ...current, page: 1, sort: "status", direction: current.sort === "status" && current.direction === "asc" ? "desc" : "asc" }))}>Stare</Button><InputText aria-label="Filtru stare" value={filters.status ?? ""} onChange={(event: ChangeEvent<HTMLInputElement>) => setQuery((current) => ({ ...current, page: 1, filters: { ...current.filters, status: event.target.value } }))} placeholder="Filtru" /></div></DataTable.THeadCell><DataTable.THeadCell>Creat de</DataTable.THeadCell><DataTable.THeadCell>Actualizat</DataTable.THeadCell><DataTable.THeadCell frozen alignFrozen="right"><span className="flex items-center justify-between gap-2"><span>Acțiuni</span>{capabilities.manage && <Button iconOnly rounded size="small" aria-label="Adaugă pachet" title="Adaugă pachet" onClick={() => setCreating(true)}><i className="pi pi-plus" aria-hidden="true" /></Button>}</span></DataTable.THeadCell></DataTable.THeadRow></DataTable.THead><DataTable.TBody>{({ item, index }) => { const entry = item as unknown as Package; const transition = entry.status === "draft" ? "submit" : entry.status === "submitted" ? "validate" : entry.status === "validated" ? "complete" : undefined; return <DataTable.Row key={entry.id} index={index}><DataTable.Cell>{scopes.find((scope) => scope.value === entry.scope)?.label ?? entry.scope}</DataTable.Cell><DataTable.Cell><Tag value={statusLabel(entry.status)} severity={statusSeverity(entry.status)} /></DataTable.Cell><DataTable.Cell>{entry.created_by_subject}</DataTable.Cell><DataTable.Cell>{entry.completed_at ?? entry.validated_at ?? entry.submitted_at ?? entry.created_at}</DataTable.Cell><DataTable.Cell frozen alignFrozen="right"><div className="flex gap-1"><Button iconOnly rounded size="small" variant="text" aria-label={`Vezi dovezi ${entry.scope}`} title="Vezi dovezi" onClick={() => setEvidenceID(entry.id)}><i className="pi pi-eye" aria-hidden="true" /></Button>{capabilities.manage && transition && <Button iconOnly rounded size="small" variant="text" aria-label={`${transition} ${entry.scope}`} title={transition} onClick={() => void advance(entry, transition)}><i className="pi pi-arrow-right" aria-hidden="true" /></Button>}</div></DataTable.Cell></DataTable.Row>; }}</DataTable.TBody></DataTable.Table></DataTable.Root>}</div><div className="sticky bottom-0 z-10 flex flex-wrap items-center justify-between gap-2 border-t border-surface pt-2"><span>{page.total ? `${(page.page - 1) * page.pageSize + 1}–${Math.min(page.page * page.pageSize, page.total)} din ${page.total}` : "0 rezultate"}</span><div className="flex items-center gap-2"><Select.Root value={String(query.pageSize)} options={[10, 20, 50].map((value) => ({ label: `${value}/pagină`, value: String(value) }))} optionLabel="label" optionValue="value" onValueChange={(event: SelectValueChangeEvent) => setQuery((current) => ({ ...current, page: 1, pageSize: Number(event.value) }))}><Select.Trigger aria-label="Rezultate pe pagină"><Select.Value /><Select.Indicator /></Select.Trigger><Select.Portal><Select.Positioner><Select.Popup><Select.List /></Select.Popup></Select.Positioner></Select.Portal></Select.Root><Button size="small" severity="secondary" variant="outlined" disabled={loading || query.page <= 1} onClick={() => setQuery((current) => ({ ...current, page: current.page - 1 }))}>Anterior</Button><Button size="small" severity="secondary" variant="outlined" disabled={loading || query.page >= lastPage} onClick={() => setQuery((current) => ({ ...current, page: current.page + 1 }))}>Următor</Button></div><span>{loading ? "Se actualizează…" : ""}</span></div></>}</div>{creating && <CreatePackageWizard portfolioID={portfolioID} api={api} onClose={() => setCreating(false)} onSaved={load} />}{evidenceID && <EvidenceDialog portfolioID={portfolioID} packageID={evidenceID} api={api} onClose={() => setEvidenceID(undefined)} />}</Card.Content></Card.Body></Card.Root>;
}
